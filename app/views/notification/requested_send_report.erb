このメールは，<%= h @requested_matter.name -%>様からの依頼により，「DECO」から発信しております。

<%= h @requested_matter.request_matter.name %> 様

あなた宛てのファイルが次のとおり，<%= h @requested_matter.name -%>様から送信されております。

■メッセージ
<%= h @requested_matter.message %>

■送信ファイル一覧
<%- @attachments.each.with_index do | attachment, count | -%><%- if attachment.virus_check == '0' -%>
  <%= count + 1 %>.<%= attachment.name -%>（<%= show_file_size(attachment) -%>）<%- end -%><%- end -%>

ダウンロードは以下のURLをクリックして行ってください。

<%= h @url %>

<%= (Time.parse(@requested_matter.file_up_date.to_s) +
                @requested_matter.file_life_period).strftime("%Y年%m月%d日%H時%M分%S秒") %>　までにダウンロードしてください。

<% if @requested_matter.password_notice == 0 -%>
なお，ダウンロードするには，パスワードが必要です。
<% elsif @requested_matter.password_notice == 1 -%>
なお，ダウンロードするには，次のパスワードが必要になります。
パスワード：<%= h @requested_matter.receive_password -%>
<% end -%>
