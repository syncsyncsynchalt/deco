このメールは，<%= h @request_matter.name %>様からの依頼により，<%= @mail_content %>「DECO」から発信しております。

<%= h @receiver %> 様

<%= h @request_matter.name %> 様からファイル送信依頼が来ております。

■依頼ID
<%= @request_matter.id %>

■送信者へのメッセージ
<%= h @request_matter.message %>

下の送信案内ページよりファイル送信をお願いします。
<%= h @url %>

<% if @request_matter.sent_at.present? %>
<%= (Time.parse(@request_matter.sent_at.to_s) +
                      @request_period).strftime("%Y年%m月%d日%H時%M分%S秒") %>　までに送信してください。
<% elsif @send_matter.moderate_flag == nil %>
<%= (Time.parse(@request_matter.created_at.to_s) +
                      @request_period).strftime("%Y年%m月%d日%H時%M分%S秒") %>　までに送信してください。
<% end %>

なお，システムを利用するには，パスワードが必要になります。
