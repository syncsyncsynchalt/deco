このメールは，<%= @mail_content %>「DECO」から発信しております。

<%= @request_matter.name %> 様

次の内容で送信完了しました。

■依頼ID
<%= @request_matter.id %>

■送信者一覧
<% @requested_matters.each.with_index do | receiver, count | %>
<%= count + 1 %> <%= receiver.name %> 様（<%= receiver.mail_address %>）
<%= @url %><%= receiver.url %> (pw:<%= receiver.send_password %>)

<% end -%>
■送信者へのメッセージ
<%= @request_matter.message %>

